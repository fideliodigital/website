"use strict";
// Scrollbar
var Scrollbar = ()(),  = (void 0)["use strict"];
// Handle Scrollbar
var handleScrollbar = ()(), $ = (void 0).$;
('.js__scrollbar').mCustomScrollbar({
    theme: 'minimal'
});
return {
    init: ()()
};
{
    handleScrollbar(); // initial setup for Scrollbar
}
();
$(document).ready(()(), {
    Scrollbar: Scrollbar,
    : .init()
});
