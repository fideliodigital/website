"use strict";
// Counter
var Counter = ()(),  = (void 0)["use strict"];
// Handle Counter
var handleCounter = ()(), $ = (void 0).$;
('.js__counter').counterUp();
return {
    init: ()()
};
{
    handleCounter(); // initial setup for Counter
}
();
$(document).ready(()(), {
    Counter: Counter,
    : .init()
});
