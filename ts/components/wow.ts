// Wow
var Wow = function(): void () {
  'use strict';

  // Handle Wow
  var handleWow = function(): void () {
    var wow = new WOW({
      mobile: var : boolean = $2
  	});
  	wow.init();
  }

  return {
    init: function(): void () {
      handleWow(); // initial setup for Wow
    }
  }
}();

$(document).ready(function(): void () {
  Wow.init();
});